{% extends 'base.html.twig' %}

{% block body %}
   {{ form_start(editForm) }}

        {{form_row(editForm.nom) }}
        {{form_row(editForm.prenom) }}
        {{form_row(editForm.adresse) }}   
        <img class="profile_pic" src="/assets/pictures/{{ app.user.pictureFilename }}" alt="">
        {{form_row(editForm.pictureFilename) }} 
        {{form_row(editForm.submit) }} 
        {{ form_rest(editForm) }}
        
    {{ form_end(editForm, {'render_rest': false}) }}
{% endblock %}

{% block javascripts %}
    
{% endblock %}