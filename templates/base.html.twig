<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
            <link rel="stylesheet" href="{{ asset('assets/css/styles.css') }}">
        {% endblock %}
    </head>
    <body>
            <nav class="navbar navbar-expand-lg navbar-light bg-warning">
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarColor01">
                    <ul class="navbar-nav mr-auto">
                        {# <li class="nav-item">
                            <a class="nav-link" href="{{ path('showListeFormations') }}">Formations</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('showListeModules') }}">Catégories et modules</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('showListeFormateurs') }}">Formateurs</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('showListeStagiaires') }}">Stagiaires</a>
                        </li> #}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                        </li> 
                        {# {% if is_granted('ROLE_USER') %} #}
                        <li class="nav-item">
                            <a class="nav-link" href="{{path('app_logout')}}">Déconnexion</a>
                        </li>
                        {# {% endif %} #}
                        {# {% if is_granted('ROLE_ADMIN') %} #}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_register_eleve') }}">Inscription élève</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_register_prof') }}">Inscription prof</a>
                        </li>
                        {# <li class="nav-item">
                            <a class="nav-link" href="{{ path('user_get_all') }}">Utilisateurs</a>
                        </li>
                        {% endif %} #}

                    </ul>
                </div>
            </nav>
                    {% block body %}

        {% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
